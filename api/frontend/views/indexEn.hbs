<!DOCTYPE html>
<html lang="uz">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Chipta Qidirish</title>
    <link rel="stylesheet" href="../assets/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">
    <link type="image/x-icon" href="../assets/images/favicon-3.png" rel="icon" />
    <link rel="stylesheet" href="https://site-assets.fontawesome.com/releases/v6.7.2/css/all.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <style>
        .popup {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.5);
            display: none;
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }

        .popup-content {
            background: white;
            padding: 20px;
            border-radius: 5px;
            text-align: center;
            max-width: 300px;
        }

        #closePopup {
            margin-top: 10px;
            padding: 8px 20px;
            background: #007bff;
            color: white;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
    </style>
</head>

<body>

    <!-- Boshqa elementlardan keyin -->
    <div id="errorPopup" class="popup" style="display: none;">
        <div class="popup-content">
            <p id="popupMessage"></p>
            <button id="closePopup">Close</button>
        </div>
    </div>

    <div class="background"></div>
    <header class="header container">
        <a href="/" style="color: rgba(0, 0, 0, 0.486); text-decoration: none;">
            <h1><span>Lime</span>Go</h1>
        </a>
        <div class="about">
            <a href="tel:+998998483737">Technical support: +998 (99) 848-37-37</a>
            <a href="/login"><button type="submit" id="login-btn"><i class="fa-regular fa-right-to-bracket"></i>
                    Login</button></a>
            <span id="user-name" style="display: none;">
                <span id="username"></span>
            </span>
            <div id="user-menu" class="dropdown-menu">
                <a href="/profile" id="profile"><i class="fa-solid fa-user"></i> Profile</a>
                <a href="/ticket" id="tickets"><i class="fa-solid fa-ticket"></i> My tickets</a>
                <a href="/logout" id="logout"><i class="fa-solid fa-right-from-bracket"></i> Log out</a>
            </div>
        </div>
    </header>

    <main class="container">
        <h1>
            Buy tickets for bus trips across Uzbekistan <span>ONLINE</span>!
        </h1>

        <p>Choose the region and date that suits you!</p>
        <form id="ticket-search">
            <div>
                <select name="from" id="from" required>
                    <option disabled selected value="">Select a station</option>
                </select>
            </div>
            <div>
                <select name="to" id="to" required>
                    <option disabled selected value="">Select a station</option>
                </select>
            </div>
            <div>
                <input type="text" name="departure_date" id="departure_date" placeholder="Select a day" required
                    disabled>
            </div>
            <button type="submit" class="search-button">Ticket search</button>
        </form>

        <div id="Data">
            <!-- Backenddan kelgan ma'lumotlar shu joyda ko'rsatiladi -->
        </div>
    </main>

    <script src="https://cdn.jsdelivr.net/npm/flatpickr"></script>
    <script src="../js/indexEn.js" defer></script>
</body>

</html>